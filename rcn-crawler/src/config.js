const DEBUG_LINKS = true;
const MAX_PAGES = 20;
const DELAY_MS = 1000;
const TIMEOUT_MS = 8000;
const TOR_HOST = "127.0.0.1";
const TOR_PORTS = [9050, 9150];

module.exports = {
    DEBUG_LINKS,
    MAX_PAGES,
    DELAY_MS,
    TIMEOUT_MS,
    TOR_HOST,
    TOR_PORTS
};